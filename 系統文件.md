# 崇正國樂團智能系統 系統文件

本系統是為「崇正國樂團」量身打造的內部管理與簽到系統。結合 Google Sheets 作為後端資料庫，Google Apps Script (GAS) 作為 API 伺服器，前端則採用基於 Tailwind CSS 的單頁 Web 應用程序 (SPA)，並支持 PWA 離線漸進式安裝與 FCM 雲端推播通知。

---

## 📂 專案檔案結構

1. **`index.html`** (前端 SPA 網頁)
   - 整個系統的主介面，包含六個主要分頁交互：首頁（個人資料）、紀錄（簽到與歷史紀錄）、組員（聲部組長/行政點名區）、廣播（活動發布/公告）、問卷庫、表演時數。
   - 使用 Tailwind CSS 外觀樣式、Lucide 做為圖示庫。
2. **`GAS.gs`** (Google Apps Script 後端腳本)
   - 接收前端 `fetch` 傳送的 POST 請求，執行對應的資料庫（Google Sheet）讀寫操作。
3. **`firebase-messaging-sw.js`**
   - 瀏覽器 Service Worker。用於處理背景狀態下的 Firebase Cloud Messaging (FCM) 推播通知接收。
4. **`manifest.json`**
   - PWA 的配置文件，定義了應用名稱、啟動 URL、背景顏色、主題色與圖示（`icon-192.png`、`icon-512.png`）。

---

## 📊 Google Sheets 資料庫表結構

後端工作簿中的頁籤（Sheets）扮演資料庫角色，有以下主要分頁：

*   **`Members`** (成員資料表)
    - 欄位：`UID` | 姓名(`Name`) | 組別(`Section`) | 帳號/Email | 密碼 | 角色(`Role`) | 主要樂器(`Instrument`) | 入團日期(`joinDate`) | 備註
    - *角色權限分為*：`Admin`（系統管理員）、`Officer`（行政幹部）、`Leader`（各部組長）、`Member`（普通團員）。
*   **`Attendance`** (簽到與請假表)
    - 欄位：時間戳記 | 姓名 | 組別 | Email | 活動名稱 | 日期 | 狀態(已簽到/請假) | 簽到時間 | 時數長度 | 請假/補簽備註
*   **`Events`** (團練/加練活動表)
    - 欄位：活動ID | 活動名稱 | 日期 | 開始時間 | 結束時間 | 地點 | 類型 | 狀態(啟用/停用)
*   **`SystemConfig`** (系統全域配置)
    - 控制目前是否有活動正處於點名狀態，以及點名的起訖時間、經緯度定位坐標和演出授權碼。
*   **`Announcements`** (公告發布紀錄)
*   **`SurveyTemplates`** / **`SurveyResponses`** (演出/出席問卷模板與回覆)
*   **`PerformanceRecords`** (表演時數紀錄表)
    - 欄位：`Timestamp` | 表演名稱 | 表演日期 | 時長(分鐘) | 團員Email | 團員姓名

---

## ⚙️ 核心功能模組

### 1. 簽到與請假 (地區定位防弊)
- 簽到時，前端會調用 HTML5 Geolocation API 獲取使用者當前經緯度，並計算與預設的「**崇正寶宮**(24.133658, 120.717338)」距離是否在 **500米** 以內。
- 若超出距離，團員必須輸入管理員在後台設定的**「演出授權碼」**才能繞過定位完成簽到。
- 行程若還沒發生（未來行程），團員可提出「請假申請」，請假後該場活動就不會計入其缺席次數。

### 2. 即時出缺席監控 (組長與幹部功能)
- 各部組長或行政人員在點名進行時，可直接查看當前「已簽到」、「未簽到（尚未簽到）、「請假」的名單。
- 賦予組長**「代簽（代點名）」**權限，可直接在監控列表點擊「代簽」，幫未攜帶手機或無法操作的組員補辦簽到。
- 成員的缺席數採取動態安全計算：會主動**過濾排除該團員入團日期（`joinDate`）之前的活動**，且**不採計未來的活動**，確保數據精確不失真。

### 3. 表演時數 (小團體/演出時數登錄)
- 行政人員 (Admin/Officer) 隨時可以在「表演」頁籤新增小團體或大型演出的「表演時數」。
- 幹部可以直接輸入「小時數」，並勾選參與演出的人員名單發布。
- 系統會自動加總每位團員的歷史表演累計時數，並在個人「表演時數」面板以視覺化圓卡顯示。

### 4. PWA 離線安裝與 FCM 推播
- 支援行動裝置「新增至主畫面」安裝為桌面 App 規格。
- 整合 Firebase Cloud Messaging 發送推播，管理員在後台發布活動或公告時，可一鍵推播提醒全團人員。
